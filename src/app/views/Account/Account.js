import {Text, SafeAreaView} from 'react-native';
import React from 'react';

const Account = () => {
  return (
    <SafeAreaView>
      <Text>Account</Text>
    </SafeAreaView>
  );
};

export default Account;
